// Contractor Site Tune-Up — form config.
// RELAY_URLS: public endpoints the form posts leads to (in order of attempt).
// If every endpoint fails, the form falls back to a pre-filled email.
// When the local relay tunnel restarts with a new URL, update the first
// entry here and re-push (see README.md "Keep the form alive").
const RELAY_URLS = [
  "https://ban-cdna-strike-wives.trycloudflare.com"
];
